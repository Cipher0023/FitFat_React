import React from 'react'

const navbar = () => {
  return (
    //using current stile
    <div className="bg-background text-foreground">
        <button className="bg-primary text-surface">Novo sistema</button>
    </div>
  )
}

export default navbar